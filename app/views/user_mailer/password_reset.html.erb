<h1>Reset le Password</h1>

<p>
  RESET le Password by clicking the link below:
</p>

<%= link_to "Reset le Password", edit_password_reset_url(@user.reset_token, email: @user.email) %>

<p>This link expires in two hours, so get on it. </p>

<p>
If you did not request to reset le Password, ignore this missive and le Password will remain unchanged.
</p>